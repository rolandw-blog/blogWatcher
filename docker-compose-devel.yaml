# This version mounts the entire project to the host files system for development
# Run me with:
# docker-compose -f docker-compose-devel.yaml up -d
version: "3"
services:
    website:
        container_name: blogwatcher
        image: blogwatcher
        environment:
            - VIRTUAL_HOST=watch.rolandw.dev
            - LETSENCRYPT_HOST=watch.rolandw.dev
            - LETSENCRYPT_EMAIL=warburtonroland@gmail.com
            - VIRTUAL_PORT=3000
            - VIRTUAL_NETWORK=proxy-network
        volumes:
            # - ./content:/usr/src/app/content
            - ./app:/usr/src/app

    mongo:
        container_name: blogwatcher_database
        image: mongo
        environment:
            # back up admin user in case something goes wrong
            - MONGO_INITDB_DATABASE=imageHost
            - MONGO_INITDB_ROOT_USERNAME=admin
            - MONGO_INITDB_ROOT_PASSWORD=rhinos
        volumes:
            # For the startup script that mongo runs to create a new user
            - ./app/docker-entrypoint-initdb.d/mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
            # Store the mongo database on the host to make it persistent between container restarts
            - ./mongo-volume:/data/db
        ports:
            - "27017:27017"
        expose:
            - 27017
        command: mongod --auth --quiet
        logging:
            driver: none

networks:
    default:
        external:
            name: nginx-proxy
